services:
  jekyll:
    build: .
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/site
      - bundle_cache:/usr/local/bundle  # caches gems between runs
    environment:
      - JEKYLL_ENV=development

volumes:
  bundle_cache: